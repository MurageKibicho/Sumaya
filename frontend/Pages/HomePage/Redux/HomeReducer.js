import { createSlice} from '@reduxjs/toolkit';
 
const HomeReducerInitialState = 
{
     
     
}
 
export const HomeReducerSlice =  createSlice({
    name: 'HomeReducer',
    initialState: HomeReducerInitialState,
    reducers :
    {
 
    }
});
 
export const { } = HomeReducerSlice.actions;